{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // Add any additional strictness options you'd like to enable.
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }

  }
}